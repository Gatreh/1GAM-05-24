extends Area2D

@onready var sign_text = $Label


func _on_body_entered(body):
	pass 
	# Show the sign text. Can be done in sign_text.visible or manipulating that through an animation.


func _on_body_exited(body):
	pass # Replace with function body.
	# Hide the sign text
